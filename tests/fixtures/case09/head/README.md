# stats

A tiny descriptive-statistics helper module.

- `mean(values)` — arithmetic mean (raises `ValueError` on empty input)
- `total(values)` — sum of values
- `median(values)` — median; averages the two middle values for even-length input (raises `ValueError` on empty input)
